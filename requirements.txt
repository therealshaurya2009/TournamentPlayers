# Web scraping and browser automation
selenium>=4.15

# Flask web framework (if you're serving a web app)
flask

# PDF generation
reportlab

# HTTP requests
requests

# Production server for Flask (Render uses this)
gunicorn

# Optional: concurrency
concurrent-log-handler
